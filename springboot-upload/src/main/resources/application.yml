spring:
  thymeleaf:
    prefix: classpath:/templates/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    mode: HTML5
    cache: false

  servlet:
    multipart:
      #是否支持多文件上传
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB